import { TransactionStatus } from './transaction-status.types';

export interface Transaction {
  amount: number;
  createdAt: string;
  currency: string;
  description: string;
  id: string;
  status: TransactionStatus;
}

export interface Transaction {
  amount: number;
  createdAt: string;
  currency: string;
  description: string;
  id: string;
  status: TransactionStatus;
}
